/**
 * class representtation
 */
export default class ClassRoom {
    /**
     *Insatiate new class @see {@link ClassRoom}.
     * @param {Number} maxStudentsSize - Return the total number of
     * student in a class
     */
    constructor(maxStudentsSize) {
      this._maxStudentsSize = maxStudentsSize;
    }
  }